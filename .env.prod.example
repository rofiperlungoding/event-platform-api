# Production env (tablet / server) — copy ke .env.prod, isi password beneran
POSTGRES_USER=rofi
POSTGRES_PASSWORD=changeme_strong_password
POSTGRES_DB=eventplatform
DATABASE_URL=postgresql://rofi:changeme_strong_password@db:5432/eventplatform
PORT=3000
